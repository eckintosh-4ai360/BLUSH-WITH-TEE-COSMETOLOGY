"use client";

import { FormEvent } from "react";
import { Button } from "@blush/ui/components/ui/button";
import DashboardLayout from "@/components/DashboardLayout";
import { trpc } from "@/lib/trpc";

export default function AdminFinancePage() { const utils = trpc.useUtils(); const summary = trpc.admin.financeSummary.useQuery(); const expenses = trpc.admin.expenses.useQuery(); const addExpense = trpc.admin.addExpense.useMutation({ onSuccess: () => { utils.admin.expenses.invalidate(); utils.admin.financeSummary.invalidate(); } }); const recordPayment = trpc.admin.recordStudentPayment.useMutation({ onSuccess: () => utils.admin.financeSummary.invalidate() }); async function submitExpense(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); await addExpense.mutateAsync({ title: String(data.get("title")), category: data.get("category") as "rent" | "utilities" | "salaries" | "transport" | "equipment" | "beauty_products" | "maintenance" | "marketing" | "stationery" | "cleaning" | "other", amount: Number(data.get("amount")), expenseDate: new Date(String(data.get("expenseDate"))), paymentMethod: data.get("paymentMethod") as "cash" | "mobile_money" | "bank" | "card" | "online", vendor: String(data.get("vendor") || "") || undefined }); event.currentTarget.reset(); } async function submitStudentPayment(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); await recordPayment.mutateAsync({ studentId: Number(data.get("studentId")), feeChargeId: Number(data.get("feeChargeId")) || undefined, amount: Number(data.get("amount")), paymentMethod: data.get("paymentMethod") as "cash" | "mobile_money" | "bank" | "card" | "online", transactionReference: String(data.get("reference") || "") || undefined }); event.currentTarget.reset(); } const cards = [["Income", summary.data?.income], ["Expenses", summary.data?.outgoings], ["Net", summary.data?.net], ["Outstanding fees", summary.data?.outstandingFees]]; return <DashboardLayout><div className="mx-auto max-w-6xl"><p className="eyebrow">Finance & billing</p><h1 className="mt-2 font-serif text-4xl text-[#4d4458] dark:text-[#e4f4f7]">Clear records, calmer decisions.</h1><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,value]) => <div key={String(label)} className="rounded-3xl border border-white bg-white/70 dark:border-white/10 dark:bg-white/5 p-6"><p className="text-xs uppercase tracking-[.14em] text-[#8a808f] dark:text-[#97b9c2]">{label}</p><p className="mt-3 font-serif text-3xl text-[#51465c] dark:text-[#e4f4f7]">GHS {Number(value ?? 0).toFixed(2)}</p></div>)}</div><div className="mt-7 grid gap-5 xl:grid-cols-2"><form onSubmit={submitStudentPayment} className="rounded-3xl bg-[#eee7f1] dark:bg-[#2c2334] p-6"><p className="eyebrow">Record tuition payment</p><h2 className="mt-2 font-serif text-2xl text-[#51465c] dark:text-[#e4f4f7]">Student payment</h2><div className="mt-5 grid gap-3 sm:grid-cols-2"><input required name="studentId" type="number" min="1" placeholder="Student ID" className="soft-input" /><input name="feeChargeId" type="number" min="1" placeholder="Fee charge ID (optional)" className="soft-input" /><input required name="amount" type="number" min="0.01" step="0.01" placeholder="Amount" className="soft-input" /><select name="paymentMethod" className="soft-input"><option value="cash">Cash</option><option value="mobile_money">Mobile Money</option><option value="bank">Bank</option><option value="card">Card</option><option value="online">Online</option></select><input name="reference" placeholder="Transaction reference" className="soft-input sm:col-span-2" /></div><Button type="submit" disabled={recordPayment.isPending} className="mt-5 rounded-full bg-[#5f5277] text-white">Record payment</Button></form><form onSubmit={submitExpense} className="rounded-3xl border border-white bg-white/70 dark:border-white/10 dark:bg-white/5 p-6"><p className="eyebrow">Record expenditure</p><h2 className="mt-2 font-serif text-2xl text-[#51465c] dark:text-[#e4f4f7]">New expense</h2><div className="mt-5 grid gap-3 sm:grid-cols-2"><input required name="title" placeholder="Expense title" className="soft-input" /><input required name="amount" type="number" min="0.01" step="0.01" placeholder="Amount" className="soft-input" /><select name="category" className="soft-input"><option value="rent">Rent</option><option value="utilities">Utilities</option><option value="salaries">Salaries</option><option value="beauty_products">Beauty products</option><option value="equipment">Equipment</option><option value="marketing">Marketing</option><option value="other">Other</option></select><input required name="expenseDate" type="date" className="soft-input" /><input name="vendor" placeholder="Vendor (optional)" className="soft-input" /><select name="paymentMethod" className="soft-input"><option value="cash">Cash</option><option value="mobile_money">Mobile Money</option><option value="bank">Bank</option><option value="card">Card</option><option value="online">Online</option></select></div><Button type="submit" disabled={addExpense.isPending} className="mt-5 rounded-full bg-[#5f5277] text-white">Save expense</Button></form></div><section className="mt-7 rounded-3xl border border-white bg-white/70 dark:border-white/10 dark:bg-white/5 p-6"><h2 className="font-serif text-2xl text-[#51465c] dark:text-[#e4f4f7]">Recent expenses</h2><div className="mt-5 grid gap-3">{expenses.data?.map(expense => <div key={expense.id} className="flex items-center justify-between rounded-2xl bg-[#faf8fb] dark:bg-[#ffffff0d] px-4 py-3"><div><p className="text-sm font-semibold text-[#584f63] dark:text-[#e4f4f7]">{expense.title}</p><p className="text-xs text-[#837a8a] dark:text-[#97b9c2]">{expense.category} · {expense.vendor || "No vendor"}</p></div><p className="font-serif text-lg text-[#5d5268] dark:text-[#e4f4f7]">GHS {Number(expense.amount).toFixed(2)}</p></div>) || <p className="text-sm text-[#817889] dark:text-[#97b9c2]">No expenses recorded.</p>}</div></section></div></DashboardLayout>; }
