import DashboardLayout from "@/components/DashboardLayout";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { FormEvent } from "react";

export default function AdminFinancePage() { const utils = trpc.useUtils(); const summary = trpc.platform.admin.financeSummary.useQuery(); const expenses = trpc.platform.admin.expenses.useQuery(); const addExpense = trpc.platform.admin.addExpense.useMutation({ onSuccess: () => { utils.platform.admin.expenses.invalidate(); utils.platform.admin.financeSummary.invalidate(); } }); const recordPayment = trpc.platform.admin.recordStudentPayment.useMutation({ onSuccess: () => utils.platform.admin.financeSummary.invalidate() }); async function submitExpense(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); await addExpense.mutateAsync({ title: String(data.get("title")), category: data.get("category") as "rent" | "utilities" | "salaries" | "transport" | "equipment" | "beauty_products" | "maintenance" | "marketing" | "stationery" | "cleaning" | "other", amount: Number(data.get("amount")), expenseDate: new Date(String(data.get("expenseDate"))), paymentMethod: data.get("paymentMethod") as "cash" | "mobile_money" | "bank" | "card" | "online", vendor: String(data.get("vendor") || "") || undefined }); event.currentTarget.reset(); } async function submitStudentPayment(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); await recordPayment.mutateAsync({ studentId: Number(data.get("studentId")), feeChargeId: Number(data.get("feeChargeId")) || undefined, amount: Number(data.get("amount")), paymentMethod: data.get("paymentMethod") as "cash" | "mobile_money" | "bank" | "card" | "online", transactionReference: String(data.get("reference") || "") || undefined }); event.currentTarget.reset(); } const cards = [["Income", summary.data?.income], ["Expenses", summary.data?.outgoings], ["Net", summary.data?.net], ["Outstanding fees", summary.data?.outstandingFees]]; return <DashboardLayout><div className="mx-auto max-w-6xl"><p className="eyebrow">Finance & billing</p><h1 className="mt-2 font-serif text-4xl text-[#4d4458]">Clear records, calmer decisions.</h1><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,value]) => <div key={String(label)} className="rounded-3xl border border-white bg-white/70 p-6"><p className="text-xs uppercase tracking-[.14em] text-[#8a808f]">{label}</p><p className="mt-3 font-serif text-3xl text-[#51465c]">GHS {Number(value ?? 0).toFixed(2)}</p></div>)}</div><div className="mt-7 grid gap-5 xl:grid-cols-2"><form onSubmit={submitStudentPayment} className="rounded-3xl bg-[#eee7f1] p-6"><p className="eyebrow">Record tuition payment</p><h2 className="mt-2 font-serif text-2xl text-[#51465c]">Student payment</h2><div className="mt-5 grid gap-3 sm:grid-cols-2"><input required name="studentId" type="number" min="1" placeholder="Student ID" className="soft-input" /><input name="feeChargeId" type="number" min="1" placeholder="Fee charge ID (optional)" className="soft-input" /><input required name="amount" type="number" min="0.01" step="0.01" placeholder="Amount" className="soft-input" /><select name="paymentMethod" className="soft-input"><option value="cash">Cash</option><option value="mobile_money">Mobile Money</option><option value="bank">Bank</option><option value="card">Card</option><option value="online">Online</option></select><input name="reference" placeholder="Transaction reference" className="soft-input sm:col-span-2" /></div><Button type="submit" disabled={recordPayment.isPending} className="mt-5 rounded-full bg-[#5f5277] text-white">Record payment</Button></form><form onSubmit={submitExpense} className="rounded-3xl border border-white bg-white/70 p-6"><p className="eyebrow">Record expenditure</p><h2 className="mt-2 font-serif text-2xl text-[#51465c]">New expense</h2><div className="mt-5 grid gap-3 sm:grid-cols-2"><input required name="title" placeholder="Expense title" className="soft-input" /><input required name="amount" type="number" min="0.01" step="0.01" placeholder="Amount" className="soft-input" /><select name="category" className="soft-input"><option value="rent">Rent</option><option value="utilities">Utilities</option><option value="salaries">Salaries</option><option value="beauty_products">Beauty products</option><option value="equipment">Equipment</option><option value="marketing">Marketing</option><option value="other">Other</option></select><input required name="expenseDate" type="date" className="soft-input" /><input name="vendor" placeholder="Vendor (optional)" className="soft-input" /><select name="paymentMethod" className="soft-input"><option value="cash">Cash</option><option value="mobile_money">Mobile Money</option><option value="bank">Bank</option><option value="card">Card</option><option value="online">Online</option></select></div><Button type="submit" disabled={addExpense.isPending} className="mt-5 rounded-full bg-[#5f5277] text-white">Save expense</Button></form></div><section className="mt-7 rounded-3xl border border-white bg-white/70 p-6"><h2 className="font-serif text-2xl text-[#51465c]">Recent expenses</h2><div className="mt-5 grid gap-3">{expenses.data?.map(expense => <div key={expense.id} className="flex items-center justify-between rounded-2xl bg-[#faf8fb] px-4 py-3"><div><p className="text-sm font-semibold text-[#584f63]">{expense.title}</p><p className="text-xs text-[#837a8a]">{expense.category} · {expense.vendor || "No vendor"}</p></div><p className="font-serif text-lg text-[#5d5268]">GHS {Number(expense.amount).toFixed(2)}</p></div>) || <p className="text-sm text-[#817889]">No expenses recorded.</p>}</div></section></div></DashboardLayout>; }
