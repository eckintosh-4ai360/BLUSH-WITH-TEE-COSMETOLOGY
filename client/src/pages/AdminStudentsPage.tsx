import DashboardLayout from "@/components/DashboardLayout";
import { Badge } from "@/components/ui/badge";
import { trpc } from "@/lib/trpc";

export default function AdminStudentsPage() { const students = trpc.platform.admin.students.useQuery(); return <DashboardLayout><div className="mx-auto max-w-6xl"><p className="eyebrow">Student management</p><h1 className="mt-2 font-serif text-4xl text-[#4d4458]">Every learning journey, visible.</h1><div className="mt-8 overflow-hidden rounded-3xl border border-white bg-white/70 shadow-[0_12px_28px_rgba(86,70,102,.06)]"><div className="grid grid-cols-[1.1fr_.8fr_.8fr_.7fr] gap-4 border-b border-[#6d5c78]/10 px-6 py-4 text-[10px] font-semibold uppercase tracking-[.16em] text-[#89808f]"><span>Student</span><span>Student number</span><span>Programme</span><span>Status</span></div>{students.data?.map(({ student, enrollment, courseTitle }) => <div key={student.id} className="grid grid-cols-[1.1fr_.8fr_.8fr_.7fr] items-center gap-4 border-b border-[#6d5c78]/8 px-6 py-5 last:border-0"><div><p className="font-semibold text-[#574d63]">{student.fullName}</p><p className="mt-1 text-xs text-[#82798a]">{student.email} · {student.phone}</p></div><p className="text-sm text-[#62596d]">{student.studentNumber}</p><div><p className="text-sm text-[#62596d]">{courseTitle || "Not yet enrolled"}</p>{enrollment && <p className="mt-1 text-xs text-[#82798a]">{enrollment.progressPercent}% progress</p>}</div><Badge className="w-fit bg-[#e7f1ed] text-[#54786a] hover:bg-[#e7f1ed]">{student.status}</Badge></div>) || <p className="p-8 text-sm text-[#807889]">Approved applicants will appear here as student records.</p>}</div></div></DashboardLayout>; }
